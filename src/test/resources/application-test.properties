jpa.default.properties.hibernate.hbm2ddl.auto=none

# Pin the Test Resources Postgres container to match the deploy/Compose target (PG 16).
# Without this it defaults to postgres:latest, which drifts and can mask version-specific
# SQL/Flyway issues that wouldn't surface until production.
test-resources.containers.postgres.image-name=postgres:16